const grupoA = 100;
const grupoB = 300;
const vencedor = grupoA > grupoB ? "Grupo A Venceu" : "Grupo B Venceu";

// Para saber se um determinado codigo e uma expressao tente atribui-la a uma variavel.

const numeros = [2, 3, 4, 5, 6];
const total = numeros.filter(numero => numero > 4);

const grupoAVenceu50 = grupoA > 50 && "Grupo A vendedor";
const grupoAVenceu150 = grupoA > 150 && "Grupo A vendedor";

const active = true;
const button = active && "Botão está ativo";