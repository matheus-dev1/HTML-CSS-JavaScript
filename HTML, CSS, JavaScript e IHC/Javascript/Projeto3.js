let nome = prompt("Digite o nome: ")
let idade = prompt("Digite a idade: ")

if (idade >= 18) {
  document.write(nome + " é maior de idade!" )
}
else {
  document.write(nome + " é menor de idade!" )
}